kind: Panel
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: {{ .Release.Name }}-form-page-panel
  namespace: {{ .Release.Namespace }}
spec:
  widgetData:
    actions: {}
    items:
      - resourceRefId: form-page-panel-row
    title: {{ .Values.panel.title }}
  resourcesRefs:
    items:
      - id: form-page-panel-row
        apiVersion: widgets.templates.krateo.io/v1beta1
        name: {{ .Release.Name }}-form-page-panel-row
        namespace: {{ .Release.Namespace }}
        resource: rows
        verb: GET