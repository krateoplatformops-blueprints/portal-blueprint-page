kind: Markdown
apiVersion: widgets.templates.krateo.io/v1beta1
metadata:
  name: {{ .Release.Name }}-panel-markdown
  namespace: {{ .Release.Namespace }}
spec:
  widgetData:
    markdown: {{ tpl .Values.panel.markdown . | quote }}